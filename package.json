{
  "name": "piece-profile",
  "version": "0.1.0",
  "description": "A personal website powered by Meteor.",
  "scripts": {
    "start": "meteor --settings settings-development.json",
    "deploy": "meteor deploy piece-profile.meteor.com --settings settings-production.json"
  }
}
